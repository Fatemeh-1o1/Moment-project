import { Pin } from '../../../components/ui/Pin';import { useMemories } from '../hooks/useMemories';
export function Throwback(){const {data=[]}=useMemories();const lastYear=new Date();lastYear.setFullYear(lastYear.getFullYear()-1);const iso=lastYear.toISOString().slice(0,10);const memory=data.find(m=>m.memoryDate===iso);if(!memory)return null;return <aside className="relative mb-6 flex -rotate-[.4deg] gap-3 rounded-sm bg-paper-orange p-4 shadow-paper"><Pin color="bg-pin-gold" className="-top-2 right-6"/><span>🕰️</span><div><p className="text-xs font-bold">این روز، یک سال پیش</p><p className="mt-1 text-sm">{memory.content}</p></div></aside>}
