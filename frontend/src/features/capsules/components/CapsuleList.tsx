import {useCapsules} from '../hooks/useCapsules';import {CapsuleCard} from './CapsuleCard';export function CapsuleList(){const {data=[],isLoading}=useCapsules();if(isLoading)return <p className="text-white">نامه‌ها دارن پیدا می‌شن…</p>;if(!data.length)return <div className="py-12 text-center text-white/90"><div className="mb-2 text-3xl">✉️</div>هنوز نامه‌ای برای خود آینده‌ات ننوشتی.</div>;return <div className="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">{data.map((x,i)=><CapsuleCard key={x.id} capsule={x} index={i}/>)}</div>}
