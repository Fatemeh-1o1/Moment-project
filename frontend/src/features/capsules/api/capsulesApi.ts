import {api} from '../../../lib/api';import type {Capsule,CapsuleInput} from '../types';export const capsulesApi={list:()=>api<{capsules:Capsule[]}>('/capsules'),one:(id:string)=>api<{capsule:Capsule}>(`/capsules/${id}`),create:(x:CapsuleInput)=>api<{capsule:Capsule}>('/capsules',{method:'POST',body:JSON.stringify(x)}),remove:(id:string)=>api<void>(`/capsules/${id}`,{method:'DELETE'})};
