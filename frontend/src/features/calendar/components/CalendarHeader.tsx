import {faNumber} from '../../../lib/date';import {persianMonthNames} from '../utils/calendarUtils';export function CalendarHeader({year,month,onMove}:{year:number;month:number;onMove:(n:number)=>void}){return <div className="mb-5 flex items-center justify-between"><h2 className="font-display text-2xl text-white">{persianMonthNames[month-1]} {faNumber(year)}</h2><div className="flex gap-2"><button aria-label="ماه قبل" onClick={()=>onMove(-1)} className="focus-ring grid size-9 place-items-center rounded-full bg-white/20 text-white hover:bg-white/30">→</button><button aria-label="ماه بعد" onClick={()=>onMove(1)} className="focus-ring grid size-9 place-items-center rounded-full bg-white/20 text-white hover:bg-white/30">←</button></div></div>}
